quart>=0.19,<1.0
hypercorn>=0.17,<1.0
psycopg2-binary>=2.9,<3.0
httpx>=0.28,<1.0
# TLS root CA bundle — httpx uses certifi's bundle when the system store is
# empty (e.g. python:3.11-slim without ca-certificates), preventing
# CERTIFICATE_VERIFY_FAILED on MusicBrainz/Discogs/Last.fm calls.
certifi>=2024.0.0
PyYAML>=6.0,<7.0
mutagen>=1.47,<2.0
beautifulsoup4>=4.12,<5.0
apscheduler>=3.10,<4.0
pydantic-settings>=2.0,<3.0
structlog>=24.0,<25.0

# C-speed fuzzy string matching for Discogs/MusicBrainz title scoring
# (optional — discogs_service falls back to stdlib difflib when unavailable)
rapidfuzz>=3.0.0,<4.0.0

# Markdown rendering for the in-app /help documentation pages
markdown>=3.5,<4.0

# Database — SQLAlchemy ORM + async driver + migrations
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
alembic>=1.13,<2.0

# Resilient retries/backoff for API clients, DB acquisition and scheduler
tenacity>=9.0,<10.0

# Static analysis used by the test suite, not by the app at runtime.
# tests/test_no_undefined_names_in_production.py uses pyflakes to fail the
# build on an ``undefined name`` — the pattern behind the UnboundLocalError
# that made every artist in a full scan report "failed" while the scan moved
# on (``_singles_pass`` read before it was bound). pyflakes is authoritative
# here; a hand-rolled AST checker produced only false positives.
pyflakes>=3.2,<5.0

